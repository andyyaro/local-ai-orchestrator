"""Supervisor agent.

Receives the raw user goal, clarifies it, and routes it to the right workflow mode.
"""


def run_supervisor(goal: str) -> str:
    """Placeholder implementation for the Supervisor role."""
    return goal.strip()
