# Core HTTP and config
requests>=2.31.0
pyyaml>=6.0.1

# Phase 9 — local vector similarity extension for retrieval memory.
# Optional at runtime: memory/retriever.py falls back to a pure-Python
# brute-force cosine similarity scan if this extension fails to load on a
# given machine (see that module's _try_sqlite_vec_similarity()).
sqlite-vec>=0.1.0

# Phase 10 — HTML-to-text conversion for deep research's fetcher, so raw
# HTML/script tags never reach a prompt.
beautifulsoup4>=4.12.0

# LangGraph and LangChain Ollama integration
langgraph>=0.2.0
langchain-ollama>=0.1.0
langchain-core>=0.2.0

# Streamlit dashboard
streamlit>=1.35.0

# Development tools
pytest>=8.0.0
black>=24.0.0
